[build]
target = "index.html"
dist = "dist"

[serve]
# `addresses`, not the deprecated singular `address` — trunk 0.21 warns on it
# and will drop it.
addresses = ["127.0.0.1"]
port = 8095
open = false

[watch]
ignore = ["dist"]
watch = ["src", "../egui-widgets/src", "assets"]
